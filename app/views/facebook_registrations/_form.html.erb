<%= form_for(@facebook_registration) do |f| %>
  <% if @facebook_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facebook_registration.errors.count, "error") %> prohibited this facebook_registration from being saved:</h2>

      <ul>
      <% @facebook_registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :facebook %><br>
    <%= f.text_field :facebook %>
  </div>
  <div class="field">
    <%= f.label :facebookId %><br>
    <%= f.text_field :facebookId %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
